dependencies {
    // we use fake initial dependencies that should get aligned with existing versions so that if dependency override works,
    // the test project properly compiles
    compile group: 'org.springframework', name: 'spring-context', version: '5.1.6.NOT_A_VALID_VERSION'
    compile group: 'org.hibernate', name: 'hibernate-core', version: '5.4.2.NOT_A_VALID_VERSION'
    compile group: 'org.apache.commons', name: 'commons-lang3', version: '3.8.1'
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
