dependencies {
    // we use fake initial dependencies that should get aligned with existing versions so that if dependency override works,
    // the test project properly compiles
    implementation group: 'org.jboss.resteasy', name: 'resteasy-jaxrs', version: '3.6.3.NOT_A_VALID_VERSION'
    implementation group: 'com.google.inject', name: 'guice', version: '4.2.2'
    implementation group: 'org.apache.commons', name: 'commons-lang3', version: '3.8.1'
    testImplementation group: 'junit', name: 'junit', version: '4.12'
}
