repositories {
    mavenCentral()
    maven { url = "https://maven.repository.redhat.com/ga/" }
}

task printRepos {
    doLast {
        project.repositories.eachWithIndex { repo, i ->
            println "REPO[$i]: ${repo.name}"
        }
    }
}
